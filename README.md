## Personality Predictor Model

This folder contains the Streamlit application and its Python dependencies.

## Run the app

From this folder, install the dependencies and start Streamlit:

```bash
pip install -r requirements.txt
streamlit run app.py
```